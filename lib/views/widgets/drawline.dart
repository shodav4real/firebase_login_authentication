import 'package:flutter/material.dart';

Container drawLine({required double width, required double height}) {
  return Container(
    height: height,
    width: width,
    color: Colors.grey,
  );
}
